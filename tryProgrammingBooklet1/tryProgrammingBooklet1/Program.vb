Imports System

Module Program
    Sub Main(args As String())
        Dim FirstName As String = "Bilal"
        Dim age As Integer = 15
        Dim TestPercent As Double = 97.3

        Console.WriteLine("first name: " & FirstName & ", Age: " & age & ", TestPercent: " & TestPercent)
    End Sub
End Module
